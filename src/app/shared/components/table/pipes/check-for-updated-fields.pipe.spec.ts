import { CheckForUpdatedFieldsPipe } from './check-for-updated-fields.pipe';

describe('CheckForUpdatedFieldsPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckForUpdatedFieldsPipe();
    expect(pipe).toBeTruthy();
  });
});
